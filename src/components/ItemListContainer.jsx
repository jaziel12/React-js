import React from "react";


const ItemListContainer = ({title}) => {
    return ( 
        <div className="title">
            <h1>{title}</h1>
        </div>

    );
};
    export default ItemListContainer;